# Requirements
Chrome
Chrome Web driver http://chromedriver.chromium.org/downloads
Anaconda Python 3.x
pip install selenium

# Questions
1. I can't find the "API" number in the LAS files.  How will I confirm the LAS is correct for the api to which it's correlated?
2. Some APIs point to the same url address.  If there are multiple APIs that think they're the same url, it will duplicate the data.
